apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ include "timelynotify-server.fullname" . }}-data
  labels:
    {{- include "timelynotify-server.labels" . | nindent 4 }}
spec:
  accessModes:
    - ReadWriteOnce
  {{- if .Values.persistence.storageClass }}
  storageClassName: {{ .Values.persistence.storageClass | quote }}
  {{- end }}
  resources:
    requests:
      storage: {{ .Values.persistence.size }}