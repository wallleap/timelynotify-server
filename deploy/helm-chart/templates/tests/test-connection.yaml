apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "timelynotify-server.fullname" . }}-test-connection"
  labels:
    {{- include "timelynotify-server.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "timelynotify-server.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
